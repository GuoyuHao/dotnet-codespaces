@page "/"
@using FrontEnd.Data
@inject WeatherForecastClient ForecastClient
@inject SunsetClient SunsetClient

<PageTitle>Weather forecast</PageTitle>

<h1>Weather forecast</h1>

<p>This component demonstrates fetching data from a service.</p>

@if (forecasts == null || sunsetTimes == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Date</th>
                <th>Temp. (C)</th>
                <th>Temp. (F)</th>
                <th>Summary</th>
                <th>Sunset Time</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < forecasts.Length; i++)
            {
                var forecast = forecasts[i];
                var sunset = sunsetTimes.FirstOrDefault(s => s.Date == forecast.Date);
                <tr>
                    <td>@forecast.Date.ToShortDateString()</td>
                    <td>@forecast.TemperatureC</td>
                    <td>@forecast.TemperatureF</td>
                    <td>@forecast.Summary</td>
                    <td>@(sunset?.FormattedTime ?? "N/A")</td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    private WeatherForecast[]? forecasts;
    private SunsetInfo[]? sunsetTimes;

    protected override async Task OnInitializedAsync()
    {
        forecasts = await ForecastClient.GetForecastAsync(DateTime.Now);
        sunsetTimes = await SunsetClient.GetSunsetRangeAsync(DateOnly.FromDateTime(DateTime.Now.AddDays(1)), 5);
    }
}
